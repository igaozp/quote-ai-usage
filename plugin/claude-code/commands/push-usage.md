---
description: Render today's Claude Code & Codex CLI usage and push it to the Dot device
allowed-tools: Bash(quote-ai:*)
---

Run `quote-ai push` and report the resulting summary line back to the user.
If the command exits non-zero, surface the error message verbatim.
